{
    "build": {
      "builder": "Nixpacks"
    },
    "deploy": {
      "startCommand": "pip install -r requirements.txt && python manage.py migrate && gunicorn transcription_project.wsgi --log-file -"
    }
  }
  